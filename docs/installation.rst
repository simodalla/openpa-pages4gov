============
Installation
============

At the command line::

    $ easy_install openpa-pages4gov

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv openpa-pages4gov
    $ pip install openpa-pages4gov